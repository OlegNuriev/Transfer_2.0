# 🎨 Руководство по кастомизации сайта DriveComfort

Это подробное руководство для владельца сайта о том, как изменять различные элементы без глубоких знаний программирования.

---

## 📞 1. Изменение контактов

### Файл: `/config/contacts.ts`

Откройте этот файл и замените тестовые значения на свои:

```typescript
export const CONTACTS = {
  phone: "+79001234567",              // ← Ваш номер телефона
  phoneDisplay: "+7 (900) 123-45-67", // ← Красиво отформатированный номер
  email: "info@drivecomfort.ru",      // ← Ваш email
  telegram: "https://t.me/your_telegram", // ← Ваш Telegram
  whatsapp: "79001234567",            // ← WhatsApp (без +)
  maxAI: "https://logo-teka.com/max/", // ← Ссылка на Max messenger
  companyName: "DriveComfort",        // ← Название компании
  companyDescription: "Трансфер & Экскурсии" // ← Слоган
};
```

**💡 Важно:** После изменения этого файла контакты обновятся автоматически во всех местах сайта!

---

## 🖼️ 2. Изменение фотографий

### 2.1 Фотографии автомобиля (главная страница)

**Файл:** `/components/About.tsx`

Найдите строки 13-39 с массивом фотографий:

```typescript
const carImages = [
  {
    url: "https://images.unsplash.com/...",  // ← Вставьте ссылку на первое фото
    alt: "Экстерьер автомобиля"              // ← Описание фото
  },
  {
    url: "https://images.unsplash.com/...",  // ← Ссылка на второе фото
    alt: "Интерьер салона"                   // ← Описание
  },
  {
    url: "https://images.unsplash.com/...",  // ← Ссылка на третье фото
    alt: "Багажное отделение"                // ← Описание
  }
];
```

**Как добавить свои фото:**
1. Загрузите фото на фотохостинг (например, Imgur, Google Drive с публичным доступом)
2. Скопируйте прямую ссылку на изображение
3. Вставьте её вместо существующей ссылки

---

### 2.2 Фотографии маршрутов

**Файл:** `/pages/RoutesPage.tsx`

Для каждого маршрута найдите секцию `images` и измените ссылки:

**Пример для маршрута "Роза Хутор" (строки 37-50):**

```typescript
images: [
  {
    url: "ВАША_ССЫЛКА_НА_ФОТО_1",
    alt: "Горный курорт"
  },
  {
    url: "ВАША_ССЫЛКА_НА_ФОТО_2",
    alt: "Канатная дорога"
  },
  {
    url: "ВАША_ССЫЛКА_НА_ФОТО_3",
    alt: "Панорама гор"
  }
],
```

Повторите для всех трёх маршрутов:
- Роза Хутор (строки 37-50)
- Панорама побережья (строки 67-79)
- Водопады и каньоны (строки 96-108)

---

### 2.3 Hero-изображение страницы маршрутов

**Файл:** `/pages/RoutesPage.tsx` (строка ~169)

Найдите:

```tsx
<ImageWithFallback
  src="https://images.unsplash.com/..."  // ← Замените на вашу ссылку
  alt="Экскурсионные маршруты"
  className="w-full h-full object-cover"
/>
```

---

## 📝 3. Изменение текста

### 3.1 Название компании и слоган

**Файл:** `/components/Hero.tsx`

Найдите строки 10-18:

```tsx
<h1 className="mb-6">
  Комфортный Трансфер и Экскурсии  {/* ← Измените название */}
</h1>
<p className="text-xl mb-8 max-w-3xl mx-auto">
  Надежный трансфер из аэропорта... {/* ← Измените описание */}
</p>
```

---

### 3.2 Описание маршрутов

**Файл:** `/pages/RoutesPage.tsx`

Для каждого маршрута измените:

```typescript
{
  id: "roza-khutor",          // НЕ изменяйте ID!
  title: "Роза Хутор",        // ← Название маршрута
  duration: "6-7 часов",      // ← Длительность
  distance: "90 км",          // ← Расстояние
  description: "Посещение курорта...", // ← Описание (можно длинное)
  includes: [                 // ← Что входит в маршрут (список)
    "Канатная дорога на высоту 2320м",
    "Панорамные виды на горы",
    "Олимпийская деревня",
    // ... добавьте больше или измените
  ],
  price: "5 000"              // ← Цена (только число)
}
```

**Где найти каждый маршрут:**
- "Роза Хутор" - строки 31-59
- "Панорама побережья" - строки 60-88
- "Водопады и каньоны" - строки 89-117

---

### 3.3 Тарифы трансфера

**Файл:** `/pages/RoutesPage.tsx` (строки 328-340)

```tsx
<div className="flex justify-between items-center">
  <span className="text-gray-600">Аэропорт → Город (центр)</span>  {/* ← Направление */}
  <span className="text-blue-600">от 2 000 ₽</span>                {/* ← Цена */}
</div>
```

Измените направление и цену для всех трёх вариантов трансфера.

---

### 3.4 Преимущества компании

**Файл:** `/components/Benefits.tsx` (строки 13-50)

```typescript
const benefits = [
  {
    icon: Shield,                    // Иконка (не меняйте, если не уверены)
    title: "15+ лет опыта",          // ← Заголовок преимущества
    description: "Профессиональный..." // ← Описание преимущества
  },
  // ... остальные 5 преимуществ
];
```

Всего 6 преимуществ. Вы можете изменить текст каждого.

---

### 3.5 Отзывы клиентов

**Файл:** `/components/Testimonials.tsx` (строки 8-34)

```typescript
const testimonials = [
  {
    name: "Анна Петрова",        // ← Имя клиента
    text: "Отличный сервис!...", // ← Текст отзыва
    rating: 5,                   // ← Оценка (от 1 до 5)
    date: "октябрь 2024"         // ← Дата
  },
  // ... ещё 2 отзыва
];
```

---

## 🎨 4. Изменение цветов

### 4.1 Основные цвета сайта

**Файл:** `/styles/globals.css`

В начале файла найдите CSS-переменные:

```css
:root {
  --primary: #2563eb;      /* ← Основной синий цвет */
  --secondary: #1e40af;    /* ← Вторичный цвет */
  /* ... другие цвета */
}
```

Замените HEX-коды на нужные цвета. Можете использовать [colorpicker](https://htmlcolorcodes.com/).

---

### 4.2 Цвета кнопок мессенджеров

**Файл:** `/components/Contact.tsx`

**Telegram** (строка ~207):
```tsx
<Button className="w-full bg-blue-500 hover:bg-blue-600">
                            ↑           ↑
                         Обычный   При наведении
```

**WhatsApp** (строка ~218):
```tsx
<Button className="w-full bg-green-500 hover:bg-green-600">
```

**Max** (строка ~229):
```tsx
<Button className="w-full bg-purple-500 hover:bg-purple-600">
```

**Доступные цвета Tailwind:**
- `blue-500`, `blue-600`, `blue-700`
- `green-500`, `green-600`, `green-700`
- `purple-500`, `purple-600`, `purple-700`
- `red-500`, `orange-500`, `yellow-500`, и т.д.

---

### 4.3 Фон второй страницы

**Файл:** `/pages/RoutesPage.tsx` (строка 160)

```tsx
<div className="min-h-screen bg-gradient-to-b from-gray-50 via-white to-gray-50">
                                              ↑        ↑       ↑
                                           Сверху   Середина  Снизу
```

Измените `gray-50`, `white`, `gray-50` на другие цвета, например:
- `from-blue-50 via-white to-blue-50` - голубоватый градиент
- `from-purple-50 via-white to-purple-50` - фиолетовый

---

## ➕ 5. Добавление контента

### 5.1 Добавить новый маршрут

**Файл:** `/pages/RoutesPage.tsx`

Найдите массив `routes` (строка ~30) и добавьте в конец новый объект:

```typescript
const routes: Route[] = [
  // ... существующие маршруты ...
  {
    id: "new-route",              // Уникальный ID (латинскими буквами)
    title: "Новый маршрут",       // Название
    duration: "4-5 часов",        // Длительность
    distance: "50 км",            // Расстояние
    description: "Описание нового маршрута...",
    images: [                     // 3 фотографии
      { url: "ссылка1", alt: "описание1" },
      { url: "ссылка2", alt: "описание2" },
      { url: "ссылка3", alt: "описание3" }
    ],
    includes: [                   // Что входит
      "Пункт 1",
      "Пункт 2",
      "Пункт 3"
    ],
    price: "3 000"                // Цена
  }
];
```

---

### 5.2 Добавить новый отзыв

**Файл:** `/components/Testimonials.tsx`

```typescript
const testimonials = [
  // ... существующие отзывы ...
  {
    name: "Новый Клиент",
    text: "Текст нового отзыва...",
    rating: 5,                    // От 1 до 5
    date: "декабрь 2024"
  }
];
```

---

### 5.3 Добавить новое преимущество

**Файл:** `/components/Benefits.tsx`

Сначала импортируйте нужную иконку из lucide-react (строка 1):

```typescript
import { Shield, Users, Clock, Star, Car, Headphones, NewIcon } from "lucide-react";
                                                              ↑
                                                    Добавьте нужную иконку
```

Затем добавьте в массив `benefits`:

```typescript
{
  icon: NewIcon,                  // Иконка, которую импортировали
  title: "Новое преимущество",
  description: "Описание нового преимущества"
}
```

**Популярные иконки:**
- `Shield` - щит (безопасность)
- `Star` - звезда (качество)
- `Clock` - часы (пунктуальность)
- `Users` - люди (клиенты)
- `Car` - машина (транспорт)
- `Heart` - сердце (забота)
- `Award` - награда (премиум)

Полный список: [lucide.dev/icons](https://lucide.dev/icons)

---

## 🔧 6. Настройка карусели

### 6.1 Отключить автопрокрутку

**Файл:** `/components/About.tsx` (строки 58-66)

Найдите и **удалите** или закомментируйте этот блок:

```typescript
// useEffect(() => {
//   const timer = setInterval(() => {
//     setCurrentImageIndex((prev) => (prev + 1) % carImages.length);
//   }, 5000);
//
//   return () => clearInterval(timer);
// }, []);
```

---

### 6.2 Изменить скорость автопрокрутки

**Файл:** `/components/About.tsx` (строка 61)

```typescript
}, 5000);  // ← Время в миллисекундах (5000 = 5 секунд)
```

Измените на нужное значение:
- `3000` = 3 секунды
- `7000` = 7 секунд
- `10000` = 10 секунд

---

## 📄 7. Политика конфиденциальности

**Файл:** `/public/privacy-policy.html`

Откройте файл и замените все значения в квадратных скобках:

- `[ДАТА]` → дата последнего обновления
- `[НАЗВАНИЕ КОМПАНИИ]` → ваше юридическое название
- `[ВАШ ТЕЛЕФОН]` → контактный телефон
- `[ВАШ EMAIL]` → контактный email
- `[ВАШ АДРЕС]` → юридический адрес

**⚠️ ВАЖНО:** Обязательно проконсультируйтесь с юристом!

---

## 🔍 8. SEO (поисковая оптимизация)

### 8.1 Основные мета-теги

**Файл:** `/index.html` (строки 8-12)

```html
<title>Экскурсии - Трансфер и Экскурсии | Сочи</title>  <!-- ← Заголовок сайта -->
<meta name="description" content="Комфортный трансфер...">   <!-- ← Описание -->
<meta name="keywords" content="трансфер Сочи, экскурсии..."> <!-- ← Ключевые слова -->
```

---

### 8.2 Open Graph (для социальных сетей)

**Файл:** `/index.html` (строки 17-23)

```html
<meta property="og:url" content="https://ваш-сайт.ru/">      <!-- ← Ваш домен -->
<meta property="og:title" content="Ваш заголовок">           <!-- ← Заголовок -->
<meta property="og:description" content="Ваше описание">     <!-- ← Описание -->
```

---

## 🎯 9. Полезные советы

### 💡 Совет 1: Тестируйте на мобильных устройствах
После каждого изменения проверяйте сайт на телефоне!

### 💡 Совет 2: Сохраняйте резервные копии
Перед большими изменениями сохраните копии файлов.

### 💡 Совет 3: Меняйте по одному параметру
Не меняйте сразу много вещей - так легче найти ошибку.

### 💡 Совет 4: Используйте качественные фотографии
- Минимальный размер: 1920x1080 пикселей
- Формат: JPG или PNG
- Вес: до 500 КБ (сжимайте через [TinyPNG](https://tinypng.com/))

### 💡 Совет 5: Проверяйте ссылки
После изменения контактов проверьте, что все мессенджеры открываются корректно.

---

## ❓ Частые ошибки

### ❌ Ошибка: "Сайт не открывается"
**Решение:** Возможно, есть синтаксическая ошибка в коде. Проверьте, что:
- Все кавычки закрыты
- Все скобки закрыты
- Нет русских символов в ID маршрутов

### ❌ Ошибка: "Картинки не загружаются"
**Решение:** 
- Проверьте, что ссылки на фото рабочие (откройте в браузере)
- Убедитесь, что используете прямые ссылки на изображения

### ❌ Ошибка: "Мессенджеры не работают"
**Решение:**
- Для Telegram: проверьте правильность username
- Для WhatsApp: используйте номер БЕЗ символа `+`
- Для Max: проверьте актуальность ссылки

---

## 📞 Нужна помощь?

Если что-то не получается:

1. ✅ Перечитайте эту инструкцию
2. ✅ Проверьте раздел "Частые ошибки"
3. ✅ Попробуйте откатить последние изменения
4. ✅ Обратитесь к разработчику

---

**Удачи в кастомизации вашего сайта! 🚀**
